import * as React from 'react';

export const PlayPanel = () => {
    return (
        <div>
            <h1>Your game is running!</h1>
            <p>Please keep this window focused so we can recognize key events</p>
        </div>
    );
};
